# webapp/requirements.txt
#
# Dependencies for the Edu-Genius Streamlit web application.
#
# Install with:
#   pip install -r webapp/requirements.txt
#
# This file extends the root requirements.txt with webapp-specific packages.
# The pipeline core (RAG, retriever, etc.) is shared from src/ — no duplication.

# ── Inherit all pipeline dependencies from the root ───────────────────────────
-r ../requirements.txt

# ── Web application (webapp-only) ─────────────────────────────────────────────
streamlit>=1.35.0

